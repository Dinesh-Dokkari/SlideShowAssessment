


<style>img {
  width: 100%;
  height: 80%;
}
</style>




<input type="hidden" id="images" value="@ViewBag.images" />
<input type="hidden" id="durations" value="@ViewBag.duration" />




<img id="image" />
<script>

    var imagelist = document.getElementById("images");
    var valuestring = imagelist.value;
    const valuesArray = valuestring.split(',');
    var ImageList =[];
    for (var i = 0; i < valuesArray.length;i++){
        ImageList.push(valuesArray[i]);
    }

    var durationList = document.getElementById("durations");
    var Durationstring = durationList.value;
    const durationArray = Durationstring.split(',');
    var Durations = [];
    for (var i = 0; i < durationArray.length; i++) {
        Durations.push(durationArray[i]);
    }

    
    let lastIndex = ImageList.length - 1;
    let currentIndex = 0;
    function showImage() {
        image.src = "/Images/" + ImageList[currentIndex];
    }
    showImage();


    //setInterval(slide,(Durations[currentIndex]*100));
    setInterval(slide,5000);
    function slide() {
        let index = getNextImageIndex();
        showImage();
    }

    function getNextImageIndex() {
        currentIndex = currentIndex + 1;
        if (currentIndex >= ImageList.length) {
            currentIndex = 0;
        }
        return currentIndex;
    }



</script>